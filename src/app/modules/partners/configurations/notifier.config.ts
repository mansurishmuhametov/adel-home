import { NotifierOptions } from 'angular-notifier';

export const notifierConfig: NotifierOptions = {
    position: {
        horizontal: {
            position: 'right',
            distance: 20
        },
        vertical: {
            position: 'top'
        }
    }
};